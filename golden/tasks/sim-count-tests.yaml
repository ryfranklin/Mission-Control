id: sim-count-tests
task_type: sim          # read-only
greenfield: false
prompt: |
  Count the functions whose names start with "test_" under the tests/ directory.
  Reply with only that integer. Do not modify any files.

known_good:
  deterministic:
    outcome: completed
    applied: false
    no_changes: true
    tests: ignore
    output_contains:
      - "7"                   # test_calc(2) + test_multiply(1) + test_strings(2) + test_inventory(2)
    output_absent: []
  judge_rubric: []
